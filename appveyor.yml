environment:
  nodejs_version: "6.9.0"

platform:
  - x64

install:
  - ps: Install-Product node $env:nodejs_version $env:platform
  - node --version
  - npm --version
  - rm -rf .git/modules
  - npm i

test_script:
  - npm test
